# PLAN_V1 — post-launch fixes and animation polish

> **Status: nearly done (2026-07-11).** Code batch implemented, independently
> validated, and attributed (Parts 2.1, 2.2, 2.4, 2.5, 4.1, 4.2, **4.3**, 4.4, 4.5 +
> version 0.2.0 + `npm pkg fix`). Attribution uses x.com/unable0_ in package.json
> `author`, the README footer, and `--help`.
>
> **Not yet published**: run `npm publish` to ship 0.2.0 (autoplay, saved ✓, clean
> filenames, Windows fix, attribution) to the registry.
>
> Delete this file when remaining gated parts are done or explicitly dropped.

---

## Remaining (gated on Kamil)

### Part 1 — npm page images (the last blocker for launch)
Pick A (rename `visual_data` → public `codebase-posters`, recommended),
B (assets-only public repo), or C (strip images). Until then, npmjs.com shows
broken poster images and a 404 repository link.

### Part 3.2.7 — Poster credit line
`made with codebase-posters` in the export footer — explicit yes/no required.

### Part 2.3 — Mobile swipe
Deferred until real mobile demand.

### Part 3 — Launch playbook
After Part 1 ships: hero video (use Save video), X thread tagging @zehf, five OSS
poster posts, Show HN, r/dataisbeautiful. Posts, not code.
